# Credenciales — nunca commitear con datos reales
config/vapid.php
config/mail.php
config/db.php

# Dependencias
vendor/

# Contenido subido por usuarios (fotos de predios) — runtime, no código
config/dist/img/predios/

# Sistema
.DS_Store
Thumbs.db
*.log
